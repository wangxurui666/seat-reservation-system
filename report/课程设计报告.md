# 自习室座位预约系统 — 课程设计报告

---

## 一、系统定义

### 1. 系统定义

本系统是一个基于 Web 的**自习室/图书馆座位预约管理平台**，旨在解决高校自习座位资源紧张、占座现象普遍的问题。系统为在校学生提供在线浏览、预约、签到自习座位的完整服务，并为管理员提供自习室与座位的管理功能。

**核心价值**：通过引入"预约—签到—信用"闭环机制和数据库层面的并发控制，确保座位资源的公平分配和高效利用。

### 2. 系统说明

**目标用户**：
- **学生用户**：注册后可浏览座位地图，按日期和时段预约空闲座位，并按时签到使用
- **管理员用户**：可管理自习室和座位信息，查看违约记录和统计数据

**核心业务流程**：

1. **注册与登录**：用户填写用户名、密码、真实姓名和学号完成注册，登录后进入系统
2. **浏览座位**：按楼栋 → 自习室 → 座位三个层次查看，选择日期和时段后实时显示各座位可用状态
3. **预约座位**：点击空闲座位的"预约"按钮，系统通过数据库行级锁（`SELECT ... FOR UPDATE`）确保同一座位同一时段仅一人预约成功
4. **签到确认**：预约成功后需在 15 分钟内在线签到，超时未签到则自动标记为"缺席"
5. **信用管理**：缺席一次扣除 10 分信用分，信用分低于 60 分则禁止继续预约
6. **统计查看**：所有用户可查看时段利用率、热门座位排行和信用排行
7. **管理后台**：管理员可增删自习室和座位，查看违约记录

**技术特点**：

| 特点 | 说明 |
|---|---|
| 并发控制 | `SELECT ... FOR UPDATE` 行级锁防止多人同时预约同一座位 |
| 事务管理 | 预约、签到等关键操作使用数据库事务保证原子性 |
| 定时任务 | MySQL 事件调度器每 5 分钟扫描超时预约并自动处理 |
| 存储过程 | 3 个核心存储过程封装预约、签到、取消业务逻辑 |
| 数据库视图 | 4 个视图提供不同维度的统计数据 |
| 索引优化 | 复合索引加速座位可用性查询和个人预约查询 |
| 密码安全 | bcrypt 哈希加密，12 轮加盐 |

---

## 二、需求分析

### 1. 需求分析

#### 1.1 功能需求

| 模块 | 功能点 | 详细描述 |
|---|---|---|
| **用户管理** | 注册 | 填写用户名、密码、真实姓名、学号，检查用户名和学号唯一性 |
| | 登录/登出 | bcrypt 密码验证，Flask session 管理登录状态 |
| | 角色区分 | student（学生）和 admin（管理员）两种角色，路由级权限控制 |
| **座位浏览** | 楼栋选择 | 3 栋楼（图书馆主馆、教学楼A座、综合学习中心） |
| | 自习室浏览 | 5 个自习室，显示楼层、容量等基本信息 |
| | 座位地图 | 每个自习室按 10 列网格展示全部座位 |
| | 日期时段筛选 | 日期选择器 + 7 个时段下拉框 |
| | 状态标识 | 绿色=可预约，红色=已占用，灰色=维护中，图标标注电源/靠窗 |
| **预约管理** | 预约座位 | 检查信用分、座位状态、并发冲突后创建预约 |
| | 签到 | 15 分钟内确认到达，自动更新状态 |
| | 取消预约 | 仅可取消"已预约"状态的记录 |
| | 超时处理 | 超过 15 分钟未签到自动标记缺席并扣分 |
| **信用管理** | 信用分 | 默认 100 分，缺席扣 10 分 |
| | 预约限制 | 信用分低于 60 分禁止预约 |
| | 违约记录 | 每次缺席自动生成违约记录并关联扣分 |
| **数据统计** | 时段利用率 | 近 30 天各时段预约数和使用率 |
| | 热门座位 | 近 30 天被预约次数最多的座位 Top 20 |
| | 信用排行 | 所有学生信用分排序，含缺席次数 |
| **管理后台** | 自习室管理 | 添加新的自习室（楼栋、名称、楼层、容量） |
| | 座位管理 | 添加、删除座位，切换座位维护/可用状态 |
| | 违约查看 | 查看最近 50 条违约记录 |

#### 1.2 非功能需求

| 类型 | 要求 | 实现方式 |
|---|---|---|
| 并发安全 | 同一座位同时段仅一人预约成功 | InnoDB 行级锁 + 事务 |
| 数据一致性 | 预约相关操作原子性 | 手动事务管理（commit/rollback） |
| 查询效率 | 常用查询响应 < 100ms | 3 个复合索引 |
| 数据完整性 | 实体、参照、用户定义完整性 | 主键、外键、ENUM、UNIQUE 约束 |
| 安全性 | 密码不可逆存储 | bcrypt 哈希 12 轮加盐 |
| 可维护性 | 业务逻辑清晰分层 | MVC 架构（Flask 蓝图 + 独立模板） |

### 2. 系统逻辑模型

#### 2.1 数据流图（DFD）

**顶层数据流图**：

```
                    ┌─────────────┐
     注册/登录      │             │   查看统计
 学生 ─────────────→│  自习室座位  │←────────── 管理员
     预约/签到      │  预约系统    │   管理座位
  ←───────────────  │             │  ←───────────
     预约结果        └─────────────┘   操作结果
```

**一级细化 — 预约流程**：

```
学生
 │
 ├─→ (1) 选择日期 → (2) 选择时段 → (3) 浏览座位地图
 │                                        │
 │                              点击空闲座位"预约"
 │                                        │
 │                              ┌─ 检查信用分 ≥ 60?
 │                              │        ↓ 是
 │                              │  SELECT ... FOR UPDATE
 │                              │        │
 │                              │  检查是否有冲突预约?
 │                              │   ├─ 有 → ROLLBACK → "已被他人预约"
 │                              │   └─ 无 → INSERT → COMMIT → "预约成功"
 │                              │        │
 │                              └─ 信用 < 60 → "信用不足"
 │
 └─→ 我的预约 → (4) 15分钟内签到 / 取消预约
```

**一级细化 — 超时处理（后台自动）**：

```
MySQL 事件调度器（每 5 分钟）
 │
 ├─ 扫描 reservation 表
 │   WHERE status = 'reserved'
 │   AND TIMESTAMPDIFF(MINUTE, created_at, NOW()) > 15
 │
 └─ 对每条超时记录:
        ├─ UPDATE reservation SET status = 'absent'
        ├─ INSERT INTO violation (扣 10 分记录)
        └─ UPDATE user SET credit = GREATEST(credit - 10, 0)
```

#### 2.2 数据字典

| 数据项 | 类型 | 取值范围 | 说明 |
|---|---|---|---|
| 用户ID | INT | 自增正整数 | 唯一标识，主键 |
| 用户名 | VARCHAR(50) | 字母数字组合 | 唯一，用于登录 |
| 密码 | VARCHAR(255) | bcrypt 哈希 | 不可逆加密 |
| 信用分 | INT | 0-100，默认 100 | 低于 60 禁止预约 |
| 角色 | ENUM | student, admin | 控制权限 |
| 座位标签 | VARCHAR(20) | 如 "A-01" | 自习室内部编号 |
| 座位状态 | ENUM | available, maintenance | 维护中不可预约 |
| 预约状态 | ENUM | reserved, checked_in, completed, cancelled, absent | 5 种流转状态 |
| 时间段 | TIME | 08:00-22:00 | 每天 7 个时段，每段 2 小时 |
| 预约日期 | DATE | 当天起 | 可预约当天及未来日期 |

---

## 三、系统设计

### 1. 概念结构设计（E-R 模型）

#### 1.1 实体及其属性

**用户（User）**：
`{ id, username, password, real_name, student_id, credit, role, created_at }`

**楼栋（Building）**：
`{ id, name, location }`

**自习室（Room）**：
`{ id, name, floor, capacity, description }`

**座位（Seat）**：
`{ id, seat_label, has_power, near_window, status }`

**时间段（TimeSlot）**：
`{ id, slot_name, start_time, end_time }`

**预约（Reservation）**：
`{ id, reserve_date, status, created_at, checked_in_at }`

**违约记录（Violation）**：
`{ id, reason, points_deducted, created_at }`

#### 1.2 实体间联系

| 联系名 | 参与实体 | 联系类型 | 说明 |
|---|---|---|---|
| 位于 | 楼栋 — 自习室 | 1:N | 一个楼栋包含多个自习室 |
| 容纳 | 自习室 — 座位 | 1:N | 一个自习室包含多个座位 |
| 预约座位 | 用户 — 座位 | M:N（通过预约） | 一个用户可预约多个座位，一个座位可被多用户分时段预约 |
| 按时段 | 预约 — 时间段 | N:1 | 每条预约属于一个时段 |
| 产生 | 预约 — 违约记录 | 1:1 | 一次缺席产生一条违约记录 |

#### 1.3 E-R 图

```
                    ┌──────────────┐
                    │   Building   │
                    │──────────────│
                    │ id (PK)      │
                    │ name         │
                    │ location     │
                    └──────┬───────┘
                           │ 1
                           │ 包含
                           │ N
                    ┌──────┴───────┐
                    │    Room      │
                    │──────────────│
                    │ id (PK)      │
                    │ building_id(FK)│
                    │ name         │
                    │ floor        │──────┐
                    │ capacity     │      │ 1
                    │ description  │      │ 容纳
                    └──────┬───────┘      │ N
                           │ 1      ┌──────┴───────┐
                           │ 容纳   │    Seat      │
                           │ N      │──────────────│
                    ┌──────┴───────┐│ id (PK)      │
                    │  TimeSlot    ││ room_id (FK) │
                    │──────────────││ seat_label   │
                    │ id (PK)      ││ has_power    │
                    │ slot_name    ││ near_window  │
                    │ start_time   ││ status       │
                    │ end_time     │└──────┬───────┘
                    └──────┬───────┘       │ 1
                           │ 1             │
                           │ 属于          │ 预约
                           │ N             │ N
         ┌──────────┐  ┌──┴───────────┐   │
         │   User   │  │ Reservation  │───┘
         │──────────│  │──────────────│
         │ id (PK)  │←─│ user_id (FK) │
         │ username │  │ seat_id (FK) │──────┐
         │ password │  │ time_slot_id(FK)│   │ 1
         │ real_name│  │ reserve_date │      │ 产生
         │student_id│  │ status       │      │ 1
         │ credit   │  │ created_at   │ ┌────┴──────────┐
         │ role     │  │checked_in_at│ │  Violation    │
         │created_at│  └──────────────┘ │───────────────│
         └──────────┘                   │ id (PK)       │
                                        │ user_id (FK)  │
                                        │reservation_id │
                                        │ reason        │
                                        │points_deducted│
                                        │ created_at    │
                                        └───────────────┘
```

### 2. 逻辑结构设计

#### 2.1 关系模式（7 张表）

**① user（用户表）**
```sql
user(id INT PRIMARY KEY AUTO_INCREMENT,
     username VARCHAR(50) NOT NULL UNIQUE,
     password VARCHAR(255) NOT NULL,
     real_name VARCHAR(50) NOT NULL,
     student_id VARCHAR(20) UNIQUE,
     credit INT DEFAULT 100,
     role ENUM('student','admin') DEFAULT 'student',
     created_at DATETIME DEFAULT CURRENT_TIMESTAMP)
```
- 主码：id
- 候选码：username, student_id
- 说明：信用分默认 100，role 区分学生和管理员

**② building（楼栋表）**
```sql
building(id INT PRIMARY KEY AUTO_INCREMENT,
         name VARCHAR(100) NOT NULL,
         location VARCHAR(200))
```
- 主码：id

**③ room（自习室表）**
```sql
room(id INT PRIMARY KEY AUTO_INCREMENT,
     building_id INT NOT NULL,
     name VARCHAR(100) NOT NULL,
     floor INT,
     capacity INT NOT NULL,
     description TEXT,
     FOREIGN KEY (building_id) REFERENCES building(id))
```
- 主码：id
- 外码：building_id → building.id

**④ seat（座位表）**
```sql
seat(id INT PRIMARY KEY AUTO_INCREMENT,
     room_id INT NOT NULL,
     seat_label VARCHAR(20) NOT NULL,
     has_power BOOLEAN DEFAULT FALSE,
     near_window BOOLEAN DEFAULT FALSE,
     status ENUM('available','maintenance') DEFAULT 'available',
     FOREIGN KEY (room_id) REFERENCES room(id),
     UNIQUE KEY uk_room_seat (room_id, seat_label))
```
- 主码：id
- 外码：room_id → room.id
- 唯一约束：(room_id, seat_label)

**⑤ time_slot（时间段表）**
```sql
time_slot(id INT PRIMARY KEY AUTO_INCREMENT,
          slot_name VARCHAR(30) NOT NULL,
          start_time TIME NOT NULL,
          end_time TIME NOT NULL)
```
- 主码：id
- 预置数据：08:00-10:00, 10:00-12:00, ..., 20:00-22:00（共 7 个时段）

**⑥ reservation（预约表） — 核心业务表**
```sql
reservation(id INT PRIMARY KEY AUTO_INCREMENT,
            user_id INT NOT NULL,
            seat_id INT NOT NULL,
            reserve_date DATE NOT NULL,
            time_slot_id INT NOT NULL,
            status ENUM('reserved','checked_in','completed','cancelled','absent')
                  DEFAULT 'reserved',
            created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
            checked_in_at DATETIME,
            FOREIGN KEY (user_id) REFERENCES user(id),
            FOREIGN KEY (seat_id) REFERENCES seat(id),
            FOREIGN KEY (time_slot_id) REFERENCES time_slot(id),
            INDEX idx_seat_date_slot (seat_id, reserve_date, time_slot_id),
            INDEX idx_user_date (user_id, reserve_date),
            INDEX idx_status (status))
```
- 主码：id
- 外码：user_id → user.id, seat_id → seat.id, time_slot_id → time_slot.id
- 业务约束：同一座位+同一日期+同一时段最多一条有效预约（由应用层 FOR UPDATE 保证）

**⑦ violation（违约记录表）**
```sql
violation(id INT PRIMARY KEY AUTO_INCREMENT,
          user_id INT NOT NULL,
          reservation_id INT,
          reason VARCHAR(200),
          points_deducted INT DEFAULT 10,
          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
          FOREIGN KEY (user_id) REFERENCES user(id),
          FOREIGN KEY (reservation_id) REFERENCES reservation(id))
```
- 主码：id
- 外码：user_id → user.id, reservation_id → reservation.id

#### 2.2 范式分析

| 范式 | 满足情况 | 说明 |
|---|---|---|
| **1NF** | ✓ | 所有字段均为原子值，无重复组 |
| **2NF** | ✓ | 非主属性完全函数依赖于主码，无部分依赖。如 reservation 中 reserve_date 完全依赖于 (id)，而非部分依赖于其他候选码 |
| **3NF** | ✓ | 不存在传递函数依赖。如 violation 表中 points_deducted 直接依赖于主码 id，而非通过其他非主属性间接依赖 |
| **BCNF** | ✓ | 所有决定因素都是候选码。reservation 表中最关键的 (seat_id, reserve_date, time_slot_id) 组合可唯一确定一条有效预约记录，在业务语义上构成候选码 |

#### 2.3 索引设计

| 索引名 | 表 | 字段 | 类型 | 用途 |
|---|---|---|---|---|
| PRIMARY | 所有表 | id | 聚簇索引 | 主键快速定位 |
| idx_seat_date_slot | reservation | (seat_id, reserve_date, time_slot_id) | 复合索引 | 座位可用性查询：`WHERE seat_id=? AND reserve_date=? AND time_slot_id=?` |
| idx_user_date | reservation | (user_id, reserve_date) | 复合索引 | "我的预约"查询：`WHERE user_id=? ORDER BY reserve_date DESC` |
| idx_status | reservation | (status) | 单列索引 | 事件调度器扫描超时预约：`WHERE status='reserved'` |

### 3. 系统功能模块图

```
                        自习室座位预约系统
                              │
        ┌─────────┬───────────┼───────────┬──────────┐
        │         │           │           │          │
     用户模块   座位预约模块   信用模块   管理模块   统计模块
        │         │           │           │          │
    ┌───┴───┐ ┌──┴────┐  ┌───┴───┐  ┌───┴───┐ ┌───┴────┐
    │注册   │ │浏览   │  │信用分 │  │自习室 │ │时段利用│
    │登录   │ │座位   │  │查询   │  │管理   │ │率统计  │
    │登出   │ │预约   │  │违约   │  │座位   │ │热门座位│
    │session│ │签到   │  │记录   │  │增删   │ │排行    │
    │验证   │ │取消   │  │超时   │  │违约   │ │信用排行│
    └───────┘ │预约   │  │扣分   │  │查看   │ └────────┘
              └───────┘  └───────┘  └───────┘
```

### 4. 其他设计图形工具

#### 4.1 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    浏览器 (Browser)                      │
│  ┌───────────────────────────────────────────────────┐  │
│  │  login.html  register.html  index.html            │  │
│  │  my_reservations.html  stats.html  admin.html     │  │
│  │  base.html (Jinja2 模板继承)                       │  │
│  │  style.css (712行)  +  main.js                     │  │
│  └───────────────────────┬───────────────────────────┘  │
└──────────────────────────┼──────────────────────────────┘
                           │ HTTP (GET/POST)
┌──────────────────────────┼──────────────────────────────┐
│                  Flask Web 应用层                        │
│  ┌───────────────────────┴───────────────────────────┐  │
│  │              app.py (主入口 + 蓝图注册)             │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │  │
│  │  │ auth.py  │  │ seat.py  │  │  admin.py    │   │  │
│  │  │ /login   │  │ /        │  │ /admin/      │   │  │
│  │  │ /register│  │ /reserve │  │ /admin/seat/ │   │  │
│  │  │ /logout  │  │ /checkin │  │ /admin/room/ │   │  │
│  │  │          │  │ /cancel  │  │              │   │  │
│  │  │          │  │ /stats   │  │              │   │  │
│  │  └────┬─────┘  └────┬─────┘  └──────┬───────┘   │  │
│  │       └──────────────┴───────────────┘           │  │
│  └───────────────────────┬───────────────────────────┘  │
│                          │                               │
│  ┌───────────────────────┴───────────────────────────┐  │
│  │            models/db.py (数据访问层)               │  │
│  │  get_db() / query() / execute()                   │  │
│  │  PyMySQL + DictCursor + 手动事务管理               │  │
│  └───────────────────────┬───────────────────────────┘  │
└──────────────────────────┼──────────────────────────────┘
                           │ TCP/3306
┌──────────────────────────┼──────────────────────────────┐
│                  MySQL 8.0 数据库层                      │
│  ┌───────────────────────┴───────────────────────────┐  │
│  │  ┌──────────┐  ┌──────────┐  ┌───────────────┐   │  │
│  │  │  表 ×7   │  │ 视图 ×4  │  │ 索引 ×3      │   │  │
│  │  │  user    │  │ 可用性   │  │ idx_seat_date │   │  │
│  │  │  building│  │ 利用率   │  │ idx_user_date │   │  │
│  │  │  room    │  │ 热门座位 │  │ idx_status   │   │  │
│  │  │  seat    │  │ 信用排行 │  │              │   │  │
│  │  │ time_slot│  └──────────┘  └───────────────┘   │  │
│  │  │reservation│                                     │  │
│  │  │ violation│  ┌──────────────┐ ┌────────────┐   │  │
│  │  └──────────┘  │ 存储过程 ×3 │ │ 事件调度器  │   │  │
│  │                │ 预约/签到/取消│ │ 每5min扫描 │   │  │
│  │                └──────────────┘ │ 每天自动完成│   │  │
│  │                                 └────────────┘   │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

#### 4.2 预约状态转换图

```
                        ┌──────────────────┐
                        │     reserved     │
                        │   (已预约，待签到) │
                        └────────┬─────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              ↓                  ↓                  ↓
        用户主动取消         15分钟内签到        15分钟超时未签到
              ↓                  ↓                  ↓
        ┌──────────┐    ┌──────────────┐    ┌──────────┐
        │ cancelled│    │ checked_in   │    │  absent  │
        │  (已取消) │    │  (已签到)     │    │ (缺席)   │
        └──────────┘    └──────┬───────┘    └────┬─────┘
                               │                 │
                          时段结束后            自动扣除
                          (事件调度器)          10 分信用分
                               ↓                 ↓
                        ┌──────────┐    ┌──────────────┐
                        │completed │    │  violation   │
                        │ (已完成)  │    │ (违约记录)    │
                        └──────────┘    └──────────────┘
```

---

## 四、详细设计

### 4.1 数据库对象详细设计

#### 4.1.1 存储过程

**sp_create_reservation — 预约座位**

```sql
CREATE PROCEDURE sp_create_reservation(
    IN  p_user_id      INT,
    IN  p_seat_id      INT,
    IN  p_reserve_date DATE,
    IN  p_time_slot_id INT,
    OUT p_result       INT,       -- 0=成功, 1=已占用, 2=信用不足, 3=维护中
    OUT p_message      VARCHAR(100)
)
BEGIN
    DECLARE v_credit     INT;
    DECLARE v_seat_status VARCHAR(20);
    DECLARE v_existing   INT DEFAULT 0;

    START TRANSACTION;

    -- Step 1: 验证信用分
    SELECT credit INTO v_credit FROM user WHERE id = p_user_id;
    IF v_credit < 60 THEN
        SET p_result = 2; SET p_message = '信用分不足，无法预约';
        ROLLBACK;
    ELSE
        -- Step 2: 验证座位状态
        SELECT status INTO v_seat_status FROM seat WHERE id = p_seat_id;
        IF v_seat_status = 'maintenance' THEN
            SET p_result = 3; SET p_message = '该座位正在维护中';
            ROLLBACK;
        ELSE
            -- Step 3: 行级锁检查冲突
            SELECT COUNT(*) INTO v_existing
            FROM reservation
            WHERE seat_id = p_seat_id
              AND reserve_date = p_reserve_date
              AND time_slot_id = p_time_slot_id
              AND status IN ('reserved', 'checked_in')
            FOR UPDATE;

            IF v_existing > 0 THEN
                SET p_result = 1; SET p_message = '该座位已被他人预约';
                ROLLBACK;
            ELSE
                -- Step 4: 创建预约
                INSERT INTO reservation (user_id, seat_id, reserve_date, time_slot_id, status)
                VALUES (p_user_id, p_seat_id, p_reserve_date, p_time_slot_id, 'reserved');
                SET p_result = 0; SET p_message = '预约成功';
                COMMIT;
            END IF;
        END IF;
    END IF;
END
```

**sp_check_in — 签到**

```sql
CREATE PROCEDURE sp_check_in(
    IN  p_reservation_id INT,
    IN  p_user_id        INT,
    OUT p_result         INT,       -- 0=成功, 1=非本人, 2=状态异常, 3=超时
    OUT p_message        VARCHAR(100)
)
BEGIN
    DECLARE v_status      VARCHAR(20);
    DECLARE v_owner_id    INT;
    DECLARE v_created_at  DATETIME;

    SELECT status, user_id, created_at
    INTO v_status, v_owner_id, v_created_at
    FROM reservation WHERE id = p_reservation_id;

    IF v_owner_id != p_user_id THEN
        SET p_result = 1; SET p_message = '非本人预约记录';
    ELSEIF v_status != 'reserved' THEN
        SET p_result = 2; SET p_message = '当前状态无法签到';
    ELSEIF TIMESTAMPDIFF(MINUTE, v_created_at, NOW()) > 15 THEN
        -- 超时处理：标记缺席 + 记录违约 + 扣分
        START TRANSACTION;
        UPDATE reservation SET status = 'absent' WHERE id = p_reservation_id;
        INSERT INTO violation (user_id, reservation_id, reason, points_deducted)
        VALUES (p_user_id, p_reservation_id, '超时未签到', 10);
        UPDATE user SET credit = GREATEST(credit - 10, 0) WHERE id = p_user_id;
        COMMIT;
        SET p_result = 3; SET p_message = '签到超时（15分钟），已标记为缺席';
    ELSE
        UPDATE reservation SET status = 'checked_in', checked_in_at = NOW()
        WHERE id = p_reservation_id;
        SET p_result = 0; SET p_message = '签到成功';
    END IF;
END
```

**sp_cancel_reservation — 取消预约**

```sql
CREATE PROCEDURE sp_cancel_reservation(
    IN  p_reservation_id INT,
    IN  p_user_id        INT,
    OUT p_result         INT,       -- 0=成功, 1=非本人, 2=状态异常
    OUT p_message        VARCHAR(100)
)
BEGIN
    DECLARE v_status   VARCHAR(20);
    DECLARE v_owner_id INT;

    SELECT status, user_id INTO v_status, v_owner_id
    FROM reservation WHERE id = p_reservation_id;

    IF v_owner_id != p_user_id THEN
        SET p_result = 1; SET p_message = '非本人预约记录';
    ELSEIF v_status != 'reserved' THEN
        SET p_result = 2; SET p_message = '当前状态无法取消';
    ELSE
        UPDATE reservation SET status = 'cancelled' WHERE id = p_reservation_id;
        SET p_result = 0; SET p_message = '取消成功';
    END IF;
END
```

#### 4.1.2 数据库事件（定时任务）

**evt_auto_mark_absent — 超时自动标记（每 5 分钟）**

```sql
CREATE EVENT evt_auto_mark_absent
ON SCHEDULE EVERY 5 MINUTE
DO
BEGIN
    DECLARE done INT DEFAULT 0;
    DECLARE v_reservation_id INT;
    DECLARE v_user_id INT;
    DECLARE cur CURSOR FOR
        SELECT id, user_id FROM reservation
        WHERE status = 'reserved'
          AND TIMESTAMPDIFF(MINUTE, created_at, NOW()) > 15;
    DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = 1;

    OPEN cur;
    read_loop: LOOP
        FETCH cur INTO v_reservation_id, v_user_id;
        IF done THEN LEAVE read_loop; END IF;

        UPDATE reservation SET status = 'absent' WHERE id = v_reservation_id;
        INSERT INTO violation (user_id, reservation_id, reason, points_deducted)
        VALUES (v_user_id, v_reservation_id, '超时未签到（系统自动）', 10);
        UPDATE user SET credit = GREATEST(credit - 10, 0) WHERE id = v_user_id;
    END LOOP;
    CLOSE cur;
END
```

**evt_complete_reservations — 自动完成（每天）**

```sql
CREATE EVENT evt_complete_reservations
ON SCHEDULE EVERY 1 DAY
DO
    UPDATE reservation SET status = 'completed'
    WHERE status = 'checked_in' AND reserve_date < CURDATE();
```

#### 4.1.3 数据库视图

| 视图名 | SQL 核心逻辑 | 用途 |
|---|---|---|
| **v_seat_availability** | `seat CROSS JOIN time_slot LEFT JOIN reservation ON ... status IN ('reserved','checked_in')` | 笛卡尔积 + 左连接，展示每个座位在所有时段的占用情况 |
| **v_slot_usage_stats** | `time_slot LEFT JOIN reservation ... WHERE reserve_date >= DATE_SUB(CURDATE(), INTERVAL 30 DAY) ... GROUP BY slot_id` | 按时间段分组统计近30天利用率，按利用率降序 |
| **v_seat_popularity** | `seat → room → building LEFT JOIN reservation ... GROUP BY seat_id ORDER BY COUNT(r.id) DESC` | 三表连接 + 聚合，按预约次数降序排列 |
| **v_user_credit_ranking** | `user LEFT JOIN reservation ... WHERE role='student' GROUP BY user_id ORDER BY credit DESC` | 学生信用排名，含总预约数和缺席数 |

#### 4.1.4 应用层数据库访问设计

由于 PyMySQL 对存储过程的 OUT 参数支持不稳定，应用层实际采用**直接 SQL + Python 手动事务管理**：

```python
# 预约流程（controllers/seat.py）
conn = get_db()
try:
    with conn.cursor() as cur:
        # 1. 检查信用分
        cur.execute('SELECT credit FROM user WHERE id = %s', (user_id,))
        user_row = cur.fetchone()
        if not user_row: return ...

        # 2. 检查座位状态
        cur.execute('SELECT status FROM seat WHERE id = %s', (seat_id,))

        # 3. 行级锁检查冲突
        cur.execute('''
            SELECT id FROM reservation
            WHERE seat_id = %s AND reserve_date = %s
              AND time_slot_id = %s AND status IN ('reserved', 'checked_in')
            FOR UPDATE
        ''', (seat_id, date, time_slot_id))

        if cur.fetchone():
            conn.rollback()
            flash('该座位已被他人预约', 'error')
        else:
            # 4. 插入预约
            cur.execute('''INSERT INTO reservation (...) VALUES (...))''')
            conn.commit()
            flash('预约成功！请在15分钟内签到', 'success')
except Exception as e:
    conn.rollback()
```

#### 4.1.5 Session 验证机制

```python
# app.py
@app.before_request
def verify_session_user():
    """每次请求前验证 session 中的用户仍然存在"""
    if session.get('user_id'):
        from models.db import query
        user = query('SELECT id FROM user WHERE id = %s',
                     (session['user_id'],), one=True)
        if not user:
            session.clear()  # 用户被删除或数据库重建后自动清 session
```

#### 4.1.6 权限控制设计

```python
# 登录检查装饰器
def login_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        if 'user_id' not in session:
            flash('请先登录', 'error')
            return redirect(url_for('auth.login'))
        return f(*args, **kwargs)
    return decorated

# 管理员检查装饰器
def admin_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        if session.get('role') != 'admin':
            flash('需要管理员权限', 'error')
            return redirect(url_for('seat.index'))
        return f(*args, **kwargs)
    return decorated
```

---

## 五、系统实现与测试

### 1. 开发平台和工具选择

| 类别 | 选择 | 版本 | 选择理由 |
|---|---|---|---|
| **数据库** | MySQL | 8.0 | 课程指定数据库，InnoDB 引擎支持事务、行锁和外键约束 |
| **后端框架** | Flask | 3.1 | Python 轻量级 Web 框架，内置 Jinja2 模板引擎，学习成本低，适合课程设计规模 |
| **数据库驱动** | PyMySQL | 1.2 | 纯 Python 实现，无需编译 MySQL C 扩展，兼容性好 |
| **密码加密** | bcrypt | 5.0 | 安全哈希算法，自动加盐，抗彩虹表攻击 |
| **前端技术** | 原生 HTML/CSS/JS | — | 不引入 React/Vue 等框架，降低复杂度，展示前端基础能力 |
| **字体** | Google Fonts | — | Playfair Display (衬线标题) + DM Sans (无衬线正文) |
| **开发工具** | VS Code | — | Python + HTML 编辑调试 |
| **操作系统** | Windows 11 | — | 开发环境 |
| **版本控制** | Git | — | 代码版本管理 |

### 2. 系统测试

#### 2.1 功能测试用例

**用户模块**

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|---|---|---|---|---|
| T01 | 新用户注册 | 填写完整信息 → 点击注册 | 提示"注册成功"，跳转登录页 | ✓ 通过 |
| T02 | 重复用户名注册 | 使用已存在的用户名注册 | 提示"用户名已被注册" | ✓ 通过 |
| T03 | 必填项校验 | 不填用户名直接注册 | 提示"请填写所有必填字段" | ✓ 通过 |
| T04 | 正确登录 | 输入正确的用户名和密码 | 跳转首页，导航栏显示用户名 | ✓ 通过 |
| T05 | 错误密码登录 | 输入错误密码 | 提示"用户名或密码错误" | ✓ 通过 |
| T06 | 登出 | 点击导航栏"退出" | 跳转登录页，session 清空 | ✓ 通过 |
| T07 | Session 自动失效 | 数据库重建后刷新页面 | 自动跳转登录页 | ✓ 通过 |

**座位浏览与预约模块**

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|---|---|---|---|---|
| T08 | 浏览座位地图 | 登录后进入首页 | 显示各楼栋/自习室/座位，颜色区分状态 | ✓ 通过 |
| T09 | 切换日期和时段 | 选择不同日期和时段 → 点查看 | 座位状态随选择刷新 | ✓ 通过 |
| T10 | 预约空闲座位 | 点击绿色座位的"预约"按钮 | 提示"预约成功！请在15分钟内签到" | ✓ 通过 |
| T11 | 预约已占用座位 | 点击红色座位（已无预约按钮） | 无法点击 | ✓ 通过 |
| T12 | 预约维护中座位 | 点击灰色座位 | 提示"该座位正在维护中" | ✓ 通过 |
| T13 | 并发预约同一座位 | 两个浏览器同时预约同一座位 | 仅一人成功，另一人提示"已被他人预约" | ✓ 通过 |
| T14 | 签到 | 预约后立即在"我的预约"点签到 | 提示"签到成功"，状态变为已签到 | ✓ 通过 |
| T15 | 超时签到 | 预约15分钟后点签到 | 提示"签到超时"，自动标记缺席并扣分 | ✓ 通过 |
| T16 | 取消预约 | 在"我的预约"点取消 | 确认后提示"取消成功"，座位恢复可用 | ✓ 通过 |

**信用与统计模块**

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|---|---|---|---|---|
| T17 | 信用不足禁止预约 | 将信用分手动调至 50 后预约 | 提示"信用分不足，无法预约" | ✓ 通过 |
| T18 | 缺席自动扣分 | 预约超时未签到 | 信用分 -10，违约记录新增一条 | ✓ 通过 |
| T19 | 查看时段利用率 | 导航栏点"统计" | 显示 7 个时段的利用率表格和进度条 | ✓ 通过 |
| T20 | 查看热门座位 | 统计页面下拉 | 显示热门座位 Top 20 排行 | ✓ 通过 |
| T21 | 查看信用排行 | 统计页面下拉 | 显示学生信用排行，颜色区分（绿/黄/红） | ✓ 通过 |

**管理后台模块**

| 编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|---|---|---|---|---|
| T22 | 管理员访问后台 | admin 用户点"管理后台" | 显示自习室管理和违约记录 | ✓ 通过 |
| T23 | 学生无法访问后台 | 普通学生访问 /admin/ | 提示"需要管理员权限"，跳转首页 | ✓ 通过 |
| T24 | 添加座位 | 管理员选择自习室，填座位号 → 添加 | 座位列表新增一条 | ✓ 通过 |
| T25 | 切换座位状态 | 管理员点"切换状态" | 座位在可用/维护间切换 | ✓ 通过 |
| T26 | 添加自习室 | 填写自习室信息 → 添加 | 新增自习室出现在列表 | ✓ 通过 |

#### 2.2 并发测试

**测试场景**：两个用户（admin 和 wxr666）同时对同一座位（seat_id=1）、同一日期（2026-06-09）、同一时段（slot_id=1）发起预约。

**测试方法**：
1. 浏览器窗口 A：使用 admin 账号登录，已预约 seat=1
2. 浏览器窗口 B：使用 wxr666 登录，同时点击 seat=1 预约

**测试结果**：
- 窗口 A（admin）：预约成功，提示"预约成功！请在15分钟内签到"
- 窗口 B（wxr666）：预约失败，提示"该座位已被他人预约"

**技术原理**：MySQL InnoDB 引擎的 `SELECT ... FOR UPDATE` 对目标行加排他锁。当第一个事务持有锁时，第二个事务必须等待。第一个事务 COMMIT 后，第二个事务的查询返回已有记录（`cur.fetchone()` 不为 None），触发 ROLLBACK。

#### 2.3 数据完整性测试

| 测试项 | 操作 | 结果 |
|---|---|---|
| 外键约束 — 删除有预约的用户 | `DELETE FROM user WHERE id=1` | ✗ 被外键约束阻止 |
| 外键约束 — 插入不存在的自习室ID | `INSERT INTO seat (room_id, ...) VALUES (999, ...)` | ✗ 被外键约束阻止 |
| ENUM 约束 — 非法预约状态 | `INSERT INTO reservation (status, ...) VALUES ('invalid', ...)` | ✗ 被 ENUM 约束阻止 |
| UNIQUE 约束 — 同自习室重复座位号 | `INSERT INTO seat (room_id, seat_label) VALUES (1, 'A-01')` | ✗ 被唯一约束阻止 |
| UNIQUE 约束 — 重复用户名 | 注册已存在的用户名 | ✗ 提示"用户名已被注册" |

---

## 六、课程设计总结

### 收获与体会

**1. 数据库设计全流程实践**

从需求分析 → 概念设计（E-R 图）→ 逻辑设计（关系模式 + 范式分析）→ 物理设计（索引 + 存储过程 + 视图），完整走通了数据库设计的标准流程。特别是对范式理论的理解不再停留在概念层面——在设计 reservation 表时，能直观感受到如果违反 3NF（如在 reservation 中冗余 room_id），会导致座位调换自习室时的大量级联更新。

**2. 并发控制的真刀实枪**

"两人同时抢一个座位"不是教科书上的空洞场景，而是真实发生的功能需求。通过 `SELECT ... FOR UPDATE` 解决了并发冲突，对事务的隔离级别（REPEATABLE READ）和 InnoDB 锁机制（行锁 vs 表锁、共享锁 vs 排他锁）有了实操经验。

**3. 存储过程 vs 应用层 SQL 的取舍**

首先设计了完整的存储过程方案，但在 PyMySQL 驱动层遇到了 OUT 参数兼容性问题，最终改为应用层直接 SQL + 手动事务。这个过程让我理解了"技术选型需要结合驱动/框架的实际行为，而非盲目依赖数据库特性"。两种方案各有优劣：存储过程适合做数据密集型运算，应用层 SQL 更适合与 Web 框架的请求生命周期配合。

**4. 信用机制的闭环设计**

信用分不是孤立字段，而是一个闭环：预约 → 签到 → 缺席扣分 → 低分禁止预约。这种"行为 → 后果 → 约束"的设计模式在实际业务中非常常见，体现在数据库层面就是触发器/事件的联动。

### 项目亮点

1. **并发控制**：`SELECT ... FOR UPDATE` 行级锁确保同一座位同一时段仅一人预约成功
2. **自动化处理**：MySQL 事件调度器每 5 分钟扫描超时预约，自动标记缺席、记录违约、扣除信用分
3. **信用体系**：闭环的信用打分机制，违规成本与行为约束联动
4. **数据统计**：4 个数据视图从不同维度（时段、座位、用户）提供分析数据
5. **完整性保证**：7 个外键、2 个 UNIQUE 约束、3 个 ENUM 约束、3 个复合索引

### 不足与改进方向

| 不足 | 改进方向 |
|---|---|
| 页面刷新交互 | 引入 AJAX/Fetch 实现无刷新预约和实时更新 |
| 预约通知 | 引入邮件/短信提醒签到 |
| 权限粒度 | 细化为超级管理员、馆长、值班员等多角色 |
| 移动端适配 | 座位地图在手机上排列过密，需专门适配 |
| 未来日期预约 | 当前可预约未来任意日期，可限制为近 7 天 |
| 单元测试 | 使用 pytest 为控制器和数据库操作编写自动化测试 |

### 总结

本项目以**自习室座位预约**这一真实校园需求为背景，从数据库设计出发，完整实现了包含用户管理、座位地图、预约签到、信用管理、后台管理和数据统计的 Web 系统。在技术实现上，重点展现了**关系型数据库的高级特性**——行级锁的并发控制、存储过程的业务封装、事件调度器的自动化处理和视图的多维统计。通过本次课程设计，对数据库系统概论的核心内容——关系模型、范式理论、索引优化、事务与并发、存储过程与触发器——有了从理论到实践的完整认知。
